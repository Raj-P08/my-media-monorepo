{
  "name": "my-media-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Headless Media SDK & Component Library Monorepo",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace=web-app run dev",
    "build": "npm --workspace=@my-media/core run build && npm --workspace=@my-media/react run build && npm --workspace=@my-media/native run build && npm --workspace=@my-media/ui-react run build && npm --workspace=@my-media/ui-native run build && npm --workspace=web-app run build",
    "check-types": "tsc --build"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.3.3"
  }
}
